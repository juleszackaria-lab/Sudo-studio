name: 03 - Package Sudo Studio (Windows Installer)

on:
  workflow_dispatch:

jobs:
  package:
    runs-on: windows-latest
    permissions:
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Stop if no manual inputs
        run: echo "Manual workflow ready."